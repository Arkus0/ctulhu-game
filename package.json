{
  "name": "la-broma-macabra",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "art:check": "python tools/check_art.py",
    "art:contact": "python tools/contact_art.py fondos && python tools/contact_art.py escenas && python tools/contact_art.py mapas && python tools/contact_art.py retratos && python tools/contact_art.py hojas",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.2.7"
  }
}
